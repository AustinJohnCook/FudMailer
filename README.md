
  # Build Email Sender App

  This is a code bundle for Build Email Sender App. The original project is available at https://www.figma.com/design/BxG9xE4TwjsHMZbLnnwF0i/Build-Email-Sender-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  